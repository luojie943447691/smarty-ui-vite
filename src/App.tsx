import { defineComponent } from "vue";
import { Button } from "./button";

export default defineComponent({
  setup() {
    return () => (
      <>
        <Button></Button>
      </>
    );
  },
});
